# Arch Install Setup Script
Run this script on a fresh Arch install to get my setup
`git clone https://github.com/CameronAsselin/Arch-Install-Setup-Script.git` /n
`chmod +x setup.sh`
`./setup.sh`
`rm setup.sh`
